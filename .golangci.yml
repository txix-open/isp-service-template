linters:
  disable-all: true
  enable:
    - asciicheck
    - bodyclose
    - deadcode
    - dogsled
    - dupl
    - errcheck
    - funlen
    - goconst
    - gocritic
    - gocyclo
    - goerr113
    - gofmt
    - gomnd
    - goprintffuncname
    - gosimple
    - govet
    - maligned
    - nakedret
    - nestif
    - nolintlint
    - prealloc
    - rowserrcheck
    - staticcheck
    - structcheck
    - typecheck
    - unconvert
    - unparam
    - unused
    - varcheck
    - gosec
    - misspell
    - whitespace


    #  automatically disabled:
    #    - depguard # не используется
    #    - gochecknoglobals
    #    - gochecknoinits
    #    - gocognit
    #    - godox # находит тудушки, не нужно
    #    - goimports
    #    - golint # подумать, может вернуть # Не стоит
    #    - stylecheck # подумать, может вернуть # не стоит
    #    - wsl
    #в список disabled добавлены:
    #    - godot #проверка на точку в конце коммента
    #    - gomodguard # Имеет смысл только при настройке, и нам наверно не нужно https://github.com/ryancurrah/gomodguard
    #    - lll # много спорных ситуаций
    #    - testpackage #все тесты должны быть в пакете с суффиксом _test
    ###############################
    #в новой версии:
    #enable:
    #- errorlint
    #- exhaustive #смотрит на соответствие между элементами enum и swich кейсами
    #- exportloopref #for _,p := []int{1, 2, 3} { s.Bar(&p) /* If s stores the pointer, it will be bug.*/ }
    #- nlreturn #checks for a new line before return and branch statements to increase code clarity
    #- noctx #finds sending http request without context.Context
    #- sqlclosecheck # Checks that sql.Rows and sql.Stmt are closed.

    #may be enable:
    #- gofunpt #https://github.com/mvdan/gofumpt
    #-tparallel # https://github.com/moricho/tparallel

    #must be disable
    #- goheader # Добавление хедера с лицензией(
    #- exhaustivestruct # полная инициализация всех структур
    #- gci #что-то вроде goimports
    #- interfacer # DEPRECATED ()
    #- scopelint #OBSOLETED
###############################

linters-settings:
  maligned:
    suggest-new: true
  lll:
    line-length: 140
    tab-width: 1
  funlen:
    lines: 80
    statements: 40
  prealloc:
    # Report preallocation suggestions only on simple loops that have no returns/breaks/continues/gotos in them.
    # True by default.
    simple: true
    range-loops: true # Report preallocation suggestions on range loops, true by default
    for-loops: true # Report preallocation suggestions on for loops, false by default
  gocritic:
    # Enable multiple checks by tags, run `GL_DEBUG=gocritic golangci-lint run` to see all tags and checks.
    # Empty list by default. See https://github.com/go-critic/go-critic#usage -> section "Tags".
    enabled-tags:
      - diagnostic
      - performance
    settings:
      hugeParam:
        sizeThreshold: 90

#issues:
#  exclude-rules:
#    - path: _test\.go # to exclude all test files for linter errcheck
#      linters:
#        - errcheck